// definition file for Encryption class

#include <Encryption.h>

//getIndex matches the letter to that in the array 
int getIndex(char c) {

	c = tolower(c);

	for (int i = 0; i < 26; ++i)
	{
		if (_alphabet[i] == ch) {
			return i;
	}

//transform will be given 1 character at a time
char Encryption::transform(char ch)
{

	
}